<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="payMapper">

<insert id="insertPay">
		insert into mem_payments(p_name, p_price, p_day, p_user)
		values(#{p_name},#{p_price},#{p_day},#{p_user})
	</insert>
	
	<update id="firstPayUpdate">
		UPDATE members set
		mem_day = DATE_ADD(current_timestamp,INTERVAL #{month} MONTH)
		where num_id = #{num_id}
	</update>
	
	<update id="payUpdate">
		UPDATE members set
		mem_day = DATE_ADD(mem_day ,INTERVAL MONTH)
		where m_idx = #{m_idx}
	</update>



</mapper>
